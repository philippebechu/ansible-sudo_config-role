---
- name: Ensure directory exists
  file:
    path: "{{ staging_dir }}"
    owner: root
    group: root
    mode: "0750"
    state: directory

- include_tasks: deploy_file.yml
  loop: "{{ sudo_files }}"
  loop_control:
    loop_var: sudo_file_item
